<div class="container">
	<div class="row">
		<div class="col-lg-offset-1 col-lg-2">
			<p>画像</p>
		</div>
		<div class="col-lg-6">
			<%= form_for(@post) do |f| %>
			<h4>タイトル</h4>
			<%= f.text_field :title %>
			<h4>キャプション</h4>
			<%= f.text_area :caption %>
			<%= f.submit '投稿' %>
			<% end %>
		</div>
	</div>
</div>