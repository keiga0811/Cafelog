<div class="container">
	<div class="row">

		<%= render "users/userinfo", user: @user %>

		<div class="col-lg-9">
			<h2>投稿一覧</h2>
			<%= @posts.each do |post| %>
				<%= attachment_image_tag post, :image, format: 'jpeg', size: "100x100" %>
			<% end %>
		</div>
	</div>
</div>