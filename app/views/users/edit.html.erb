<div class="container">
	<div class="row">
		<%= form_for(@user) do |f| %>
			<div class="col-lg-3">
				<%= f.attachment_field :profile_image %>
			</div>
			<div class="col-lg-9">
				<p>name</p>
				<%= f.text_field :name %>
				<p>introduction</p>
				<%= f.text_area :introduction %>
				<%= f.submit "更新する" %>
			</div>
		<% end %>
	</div>
</div>
